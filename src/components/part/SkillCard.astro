---
const {skill} = Astro.props;

---
<article class="card">
    <img height="90" width="90" class="hidden min-[420px]:block w-1/3 min-[900px]:w-1/2" src={skill.logo} alt={`logo ${skill.title}`} loading="lazy"/>
    <strong class="card-text">{skill.title}</strong>
  </article>